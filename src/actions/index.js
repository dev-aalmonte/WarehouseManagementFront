import {
    login,
    logout,
    getUserByToken
} from './auth';

import {
    getProducts, 
} from './product';

export {
    // Auth
    login,
    logout,
    getUserByToken,

    // Products
    getProducts,
}