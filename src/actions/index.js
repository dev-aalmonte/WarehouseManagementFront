import {
    login,
    logout,
    getUserByToken
} from './auth';

import {
    getProducts,
    addProduct
} from './product';

export {
    // Auth
    login,
    logout,
    getUserByToken,

    // Products
    getProducts,
    addProduct
}